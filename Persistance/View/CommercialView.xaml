<UserControl x:Class="Persistance.View.CommercialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Persistance.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"  Visibility="{Binding CommercialViewModel.AffichageView}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Column="1" Margin="15" Grid.RowSpan="4"/>
        <StackPanel Orientation="Vertical" Grid.Column="0" Grid.Row="0">
            <Label Content="Bonjour"/>
            <Label Content="Nom :"/>
            <Label Content="Prenom : "/>
            <Label Content="Fonction :"/>
            <Label Content="Nom du manager :"/>
        </StackPanel>
       
    </Grid>
</UserControl>
